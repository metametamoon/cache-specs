name: Tests

on:
  workflow_dispatch:

jobs:
  tests:
    name: Run Tests
    
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - run: |
          g++ main.cpp page_traverser.cpp -g -std=gnu++20 -DVERBOSE
          ./a.out
        name: Measure Cache stats
